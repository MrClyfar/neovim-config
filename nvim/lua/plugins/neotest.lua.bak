-- vim.keymap.set('n', '<Leader>gtest', ':lua require("neotest").run.run()<CR>', { desc = 'Run test' })
return {
  "nvim-neotest/neotest",
  optional = true,
  opts = {
    log_level = vim.log.levels.DEBUG, -- Add this line
    adapters = {
      ["neotest-golang"] = {
        go_test_args = { "-v" },
        runner = "gotestsum",
        dap_go_enabled = true,
      },
    },
  },
}
